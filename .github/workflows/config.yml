name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: export CYPRESS_MAILSLURP_API_KEY=faac19f993905eb1cfbb0c214be792a8d52613fccb37b1ad612bbb5096b72313
      # Install NPM dependencies, cache them correctly
      # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          node-version: 18